export enum MatrixStateEnum {
    /**
     * Состояние игры по умолчанию
     */
    default,

    /**
     * Дополнительное состояние, сигнализирующее о том, что происходит поворот игрового поля
     */
    rotating,
}